{
  "_note": "Per-component decision guidance merged into cathode.manifest.json by gen-manifest.js. `whenToUse` is one imperative sentence; `vs` is an array of disambiguation pairs against commonly-confused siblings. Kept separate so manifest structure stays stable and writing stays focused.",

  "TerminalFrame": {
    "whenToUse": "Group a named section of readouts or controls with a visible label — the \"┌─ PEERS ─\" terminal section.",
    "vs": [
      { "component": "Card", "picker": "TerminalFrame when the section needs a visible name; Card when content speaks for itself." }
    ]
  },
  "Card": {
    "whenToUse": "Wrap any content in a bordered panel without a label — info blocks, grouped form fields, composable widgets.",
    "vs": [
      { "component": "TerminalFrame", "picker": "Card when you don't need an inset title; TerminalFrame when you do." },
      { "component": "StatusTile", "picker": "Card for freeform content; StatusTile for the standard icon + title + subtitle pattern." }
    ]
  },
  "PixelBar": {
    "whenToUse": "Show a real-time VU-meter-style level (volume, signal intensity) as discrete pixel cells.",
    "vs": [
      { "component": "ProgressBar", "picker": "PixelBar for discrete level meters; ProgressBar for continuous determinate progress (e.g. download %)." },
      { "component": "ActivityBar", "picker": "PixelBar for a known level 0–1; ActivityBar for pseudo-random \"something is happening\" animation." }
    ]
  },
  "ActivityBar": {
    "whenToUse": "Show a \"traffic / activity / thinking\" indicator where the exact level is meaningless — deterministic pseudo-random per seed.",
    "vs": [
      { "component": "PixelBar", "picker": "ActivityBar for decorative/indeterminate activity; PixelBar for a real quantitative level." },
      { "component": "Loader", "picker": "ActivityBar is part of a UI readout; Loader is a standalone \"please wait\" indicator." }
    ]
  },
  "PulsingDot": {
    "whenToUse": "Attach a small live-status signal to a label or row — LIVE / SCANNING / ONLINE.",
    "vs": [
      { "component": "Avatar", "picker": "PulsingDot for an abstract status indicator; Avatar's status dot when it's tied to a specific person." },
      { "component": "Badge", "picker": "PulsingDot for live animated state; Badge for static labels (NEW / BETA)." }
    ]
  },
  "DotLeader": {
    "whenToUse": "Render a single LABEL ……… VALUE row inside a TerminalFrame, Card, or stack of readouts.",
    "vs": [
      { "component": "Table", "picker": "DotLeader for 1–8 rows of key/value pairs; Table for multi-column tabular data." }
    ]
  },
  "StatusTile": {
    "whenToUse": "Render an MPC-style icon + title + subtitle tile in a grid of statuses or shortcut entry points.",
    "vs": [
      { "component": "Card", "picker": "StatusTile for the fixed icon/title/subtitle pattern; Card for custom content." },
      { "component": "Pill", "picker": "StatusTile is a chunky tile in a grid; Pill is a compact inline button." }
    ]
  },
  "Pill": {
    "whenToUse": "A compact icon+text button used for navigation, filters, or tabs — anything with a persistent `active` state.",
    "vs": [
      { "component": "Button", "picker": "Pill when the button represents persistent state (current tab, selected filter); Button for one-shot actions (Save, Delete)." },
      { "component": "Tag", "picker": "Pill is tappable and can be active; Tag is a static label (optionally removable)." },
      { "component": "Chips", "picker": "Pill for nav; Chips for a scrolling row of preset phrases inserted once then cleared." }
    ]
  },
  "Button": {
    "whenToUse": "Fire a one-shot action — submit, delete, cancel, run. Use `variant` for semantic weight and `ai={{ action }}` for AI-routed intents.",
    "vs": [
      { "component": "Pill", "picker": "Button for one-shot actions; Pill for nav/filter state." },
      { "component": "Menu", "picker": "Single Button for one action; Menu when the button reveals a list of actions." }
    ]
  },
  "TextField": {
    "whenToUse": "Single-line free-text input. Add `ai={{ suggest: true }}` for inline ghost-text AI completion.",
    "vs": [
      { "component": "TextArea", "picker": "TextField for single-line; TextArea for multi-line." },
      { "component": "Counter", "picker": "TextField for free-form numbers you want the user to type; Counter for bounded values nudged with arrow buttons." },
      { "component": "SearchBar", "picker": "TextField for a free-form value; SearchBar when you want a results dropdown under the input." }
    ]
  },
  "SearchBar": {
    "whenToUse": "Search/filter across a known dataset with a results dropdown. Toggle `ai={{ semantic: true }}` for provider-ranked results.",
    "vs": [
      { "component": "Select", "picker": "SearchBar for fuzzy search over a large/unknown set; Select for a finite enum picker." },
      { "component": "TextField", "picker": "SearchBar when you need a results dropdown; TextField for free text with no suggestions." }
    ]
  },
  "Chat": {
    "whenToUse": "Drop in a ready-made AI conversation UI — streaming assistant text, send/cancel, auto-scroll, hooked to a `CathodeAIProvider`.",
    "vs": [
      { "component": "TextField", "picker": "Chat for conversational back-and-forth; TextField for a single AI-suggested completion." }
    ]
  },
  "Toast": {
    "whenToUse": "Surface a short transient status — session resumed, copied to clipboard, connection lost. App controls timing.",
    "vs": [
      { "component": "Dialog", "picker": "Toast for transient confirmations; Dialog when the user must acknowledge or choose." },
      { "component": "FormField error", "picker": "Toast for global notices; FormField `error` for inline validation." }
    ]
  },
  "Dialog": {
    "whenToUse": "Block the UI until the user confirms, chooses, or dismisses — destructive confirmations, wizards, full-form panels.",
    "vs": [
      { "component": "Drawer", "picker": "Dialog for centered modal focus; Drawer for a side/top/bottom panel that doesn't fully cover the page." },
      { "component": "Popover", "picker": "Dialog is modal and portaled; Popover is anchored and non-modal." }
    ]
  },
  "Chips": {
    "whenToUse": "Render a horizontally-scrolling row of preset phrases the user inserts once — quick-replies, preset commands, saved filters.",
    "vs": [
      { "component": "Tabs", "picker": "Chips for one-shot inserts that clear; Tabs for switching which view is active." },
      { "component": "Pill", "picker": "Chips for a scroll row of presets; Pill for a single nav/action button." },
      { "component": "Tag", "picker": "Chips are tappable actions; Tag is a display-only label (optionally removable)." }
    ]
  },
  "Counter": {
    "whenToUse": "Let the user nudge a bounded numeric value up/down — WPM, volume, count. Discrete steps, welded label.",
    "vs": [
      { "component": "TextField", "picker": "Counter for bounded integer-ish values; TextField for free-form numbers." },
      { "component": "Select", "picker": "Counter for a range; Select for a finite enum." }
    ]
  },
  "Toggle": {
    "whenToUse": "Flip a single setting on/off that takes effect immediately — notifications, dark mode, debug overlays.",
    "vs": [
      { "component": "Checkbox", "picker": "Toggle for immediate settings; Checkbox for form values in a list (multi-select, indeterminate)." }
    ]
  },
  "HazardStripes": {
    "whenToUse": "Wrap a destructive or high-stakes button/zone in a caution overlay — \"ARMED · HOLD TO FIRE\", emergency stop zones.",
    "vs": [
      { "component": "Button variant=danger", "picker": "Button danger for standard destructive buttons; HazardStripes adds the extra visual weight for irreversible / armed states." }
    ]
  },
  "Checkbox": {
    "whenToUse": "A boolean form value inside a list of related options — multi-select filters, agreement boxes, parent-of-children rows.",
    "vs": [
      { "component": "Toggle", "picker": "Checkbox for form values you submit; Toggle for immediate-effect settings." },
      { "component": "RadioGroup", "picker": "Checkbox for multi-select (any can be on); RadioGroup for single-select (exactly one)." }
    ]
  },
  "RadioGroup": {
    "whenToUse": "Pick exactly one option from 2–5 visible alternatives — mode, region, plan tier.",
    "vs": [
      { "component": "Select", "picker": "RadioGroup when all options should be visible at once; Select when the list is long or space is tight." },
      { "component": "Checkbox", "picker": "RadioGroup for single-select; Checkbox for multi-select." },
      { "component": "Tabs", "picker": "RadioGroup for form values you submit; Tabs for switching which view is shown." }
    ]
  },
  "Select": {
    "whenToUse": "Pick one value from a finite enum when options are many or space is tight — theme, region, unit.",
    "vs": [
      { "component": "RadioGroup", "picker": "Select for long/hidden lists; RadioGroup when showing all options helps the user compare." },
      { "component": "SearchBar", "picker": "Select for finite enums; SearchBar for fuzzy matching over an open set." }
    ]
  },
  "TextArea": {
    "whenToUse": "Multi-line free-text input — notes, descriptions, bios. Optional character counter when `maxLength` is set.",
    "vs": [
      { "component": "TextField", "picker": "TextArea for multi-line; TextField for single-line." }
    ]
  },
  "FormField": {
    "whenToUse": "Wrap any Cathode input (TextField, TextArea, Select, Checkbox, RadioGroup, Toggle, Counter) with a label, hint, and/or error message — auto-wires aria attributes.",
    "vs": [
      { "component": "Toast", "picker": "FormField error for inline validation beside the input; Toast for out-of-band global notices." }
    ]
  },
  "Badge": {
    "whenToUse": "Attach a small inline status marker to text — NEW, BETA, v0.3.0, DEPRECATED. Display-only.",
    "vs": [
      { "component": "Tag", "picker": "Badge for status labels (solid fill by default, display-only); Tag for keywords/filters (outlined, optional remove)." },
      { "component": "PulsingDot", "picker": "Badge for static status text; PulsingDot for live animated state dot." }
    ]
  },
  "Tag": {
    "whenToUse": "Classify or filter with an accent-colored keyword marker — categories, tech stack, people. Optional × to remove.",
    "vs": [
      { "component": "Badge", "picker": "Tag for keyword/filter labels (often removable); Badge for status markers (display-only)." },
      { "component": "Chips", "picker": "Tag represents a classification; Chips is an interactive row of preset phrases." }
    ]
  },
  "Avatar": {
    "whenToUse": "Identify a person or entity — image or initials fallback in a square, optional presence dot.",
    "vs": [
      { "component": "StatusTile", "picker": "Avatar represents a person; StatusTile represents a named system or app surface." }
    ]
  },
  "Kbd": {
    "whenToUse": "Display a keyboard shortcut alongside a menu item, tooltip, or docs reference — Cmd+K, Ctrl+Shift+S.",
    "vs": [
      { "component": "CodeBlock", "picker": "Kbd for keyboard keys; CodeBlock for code." }
    ]
  },
  "CodeBlock": {
    "whenToUse": "Show a multi-line code sample in docs, prompt previews, or debug panels. Accepts plain text or pre-highlighted HTML (Shiki / Highlight.js).",
    "vs": [
      { "component": "inline <code>", "picker": "CodeBlock for multi-line; inline `<code>` for single terms in prose." }
    ]
  },
  "Table": {
    "whenToUse": "Show structured tabular data — columns, rows, optional sort, optional row-click. Consumer controls the sort state.",
    "vs": [
      { "component": "DotLeader", "picker": "Table for multi-column data; DotLeader for a handful of key/value rows." }
    ]
  },
  "Tabs": {
    "whenToUse": "Switch between related views without leaving the page — Overview / Logs / Settings. Persistent active state.",
    "vs": [
      { "component": "RadioGroup", "picker": "Tabs for switching which view is shown; RadioGroup for form values you submit." },
      { "component": "Pill", "picker": "Tabs for a compact tab row with underline; Pill for standalone nav buttons." },
      { "component": "Chips", "picker": "Tabs for persistent view state; Chips for one-shot preset phrases." }
    ]
  },
  "Breadcrumbs": {
    "whenToUse": "Show the user's location inside a nested hierarchy — Home › Docs › Tabs. Last item is the current page.",
    "vs": [
      { "component": "Tabs", "picker": "Breadcrumbs shows *where you are* in a hierarchy; Tabs switches between *peer* views." }
    ]
  },
  "Menu": {
    "whenToUse": "Reveal a list of actions from a click trigger — context menu, overflow menu, \"More actions\" dropdown. Keyboard-navigable.",
    "vs": [
      { "component": "Select", "picker": "Menu for a list of actions (each does something); Select for picking one value." },
      { "component": "Popover", "picker": "Menu for a canonical list of menu items; Popover for arbitrary panel content." }
    ]
  },
  "Pagination": {
    "whenToUse": "Let the user jump between pages of a long list — prev/next arrows + windowed page buttons with ellipses.",
    "vs": [
      { "component": "Table with scroll", "picker": "Pagination for discrete pages; unpaginated scroll when the list is short or infinite-scroll is natural." }
    ]
  },
  "Popover": {
    "whenToUse": "Show rich anchored content from a click trigger — details cards, mini-forms, property editors. Portaled, non-modal.",
    "vs": [
      { "component": "Tooltip", "picker": "Popover for rich click-triggered content; Tooltip for short hover/focus-triggered text hints." },
      { "component": "Menu", "picker": "Popover for arbitrary content; Menu for a canonical list of actions." },
      { "component": "Dialog", "picker": "Popover is anchored + non-modal; Dialog is centered + modal." }
    ]
  },
  "Tooltip": {
    "whenToUse": "Attach a short hover/focus hint to a control — keyboard shortcut, terse explanation. Text-only.",
    "vs": [
      { "component": "Popover", "picker": "Tooltip for text-only hints on hover; Popover for rich click-triggered panels." }
    ]
  },
  "Drawer": {
    "whenToUse": "Slide in a side panel from the viewport edge — filters, inspector, settings — without covering the underlying page.",
    "vs": [
      { "component": "Dialog", "picker": "Drawer for edge-anchored panels (non-modal by default); Dialog for centered modal focus." },
      { "component": "Popover", "picker": "Drawer for full-height edge panels; Popover for small anchored floaters." }
    ]
  },
  "ProgressBar": {
    "whenToUse": "Show determinate progress as a continuous bar — downloads, uploads, task %. Omit `value` for indeterminate shimmer.",
    "vs": [
      { "component": "PixelBar", "picker": "ProgressBar for completion %; PixelBar for real-time level meters." },
      { "component": "Loader", "picker": "ProgressBar when you know the %; Loader when duration is unknown." }
    ]
  },
  "Loader": {
    "whenToUse": "Indicate a general \"please wait, working on it\" state when duration is unknown.",
    "vs": [
      { "component": "ProgressBar", "picker": "Loader for unknown duration; ProgressBar for known %." },
      { "component": "Skeleton", "picker": "Loader in-place of a button/readout while it waits; Skeleton in-place of content layout before it arrives." }
    ]
  },
  "Skeleton": {
    "whenToUse": "Hold the shape of content that's still loading — row heights, card footprints — so the page doesn't jump when data arrives.",
    "vs": [
      { "component": "Loader", "picker": "Skeleton mirrors the eventual layout; Loader is a small spinner-style indicator." }
    ]
  },
  "SignalBars": {
    "whenToUse": "Display a \"strength of N\" reading — cellular bars, battery, reception level.",
    "vs": [
      { "component": "PixelBar", "picker": "SignalBars for a 1-of-N strength indicator with ascending bar heights; PixelBar for a horizontal row of equal-height cells." },
      { "component": "ProgressBar", "picker": "SignalBars for a discrete 0–5 level; ProgressBar for continuous %." }
    ]
  },
  "ScanLine": {
    "whenToUse": "Wrap a surface in a decorative CRT scan-line + sweep overlay — live camera feeds, retro dashboards.",
    "vs": [
      { "component": "HazardStripes", "picker": "ScanLine for CRT/retro styling; HazardStripes for \"caution/armed\" overlay on destructive zones." }
    ]
  },
  "TypewriterText": {
    "whenToUse": "Reveal short text character-by-character for dramatic effect — boot-up messages, AI turn-in-progress, intro hooks.",
    "vs": [
      { "component": "Chat streaming", "picker": "TypewriterText for static pre-known text; Chat's streaming handles real AI-generated tokens." }
    ]
  },
  "Countdown": {
    "whenToUse": "Count down to a specific Date in real time — launch timers, expiry countdowns, game rounds.",
    "vs": [
      { "component": "Counter", "picker": "Countdown auto-ticks toward a target Date; Counter is a manual numeric rocker." }
    ]
  },
  "Stack": {
    "whenToUse": "Replace inline `style={{ display: 'flex', gap: ... }}` scaffolding — a utility wrapper for direction + gap + align + justify + wrap.",
    "vs": [
      { "component": "Card", "picker": "Stack is structural (no chrome); Card is visual (border + optional fill)." }
    ]
  },
  "Accordion": {
    "whenToUse": "Stack several expand/collapse sections — settings groups, FAQ, long-form details panels.",
    "vs": [
      { "component": "Tabs", "picker": "Accordion shows multiple sections simultaneously (or none); Tabs shows exactly one at a time." }
    ]
  }
}
